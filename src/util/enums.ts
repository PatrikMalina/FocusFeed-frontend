export enum CustomTypes {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
}

export enum ActionTypes {
  SET_USER = 'setUser',
  REMOVE_USER = 'removeUser',
}

export enum Screens {
  HOME = 'Home',
  SIGN_IN = 'SignIn',
  SIGN_UP = 'SignUp',
}
